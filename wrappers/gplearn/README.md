# gplearn Wrapper

Status: batch 1 candidate

Source mapping:

- project wrapper already exists at `baselines/gplearn/run_gplearn_benchmark.py`
- upstream source mirror exists at `baselines/iclrcom/gplearn`

Planned action:

- migrate or delegate the existing benchmark script into this wrapper layer
- preserve current result schema if possible
